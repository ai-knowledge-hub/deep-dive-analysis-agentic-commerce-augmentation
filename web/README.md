# Agentic Commerce Web

```bash
cd web
pnpm install
pnpm dev
```

Set `NEXT_PUBLIC_API_URL` (see `.env.local.example`) to point at the FastAPI backend (default `http://localhost:8000`).
Set `NEXT_PUBLIC_CLIENT_ID` to your tenant identifier (default `default`).
