"""Infrastructure alignment adapters (thin wrappers over existing modules).

This package exists to isolate IO-ish / compatibility dependencies from the
application layer during the strangler migration.
"""
